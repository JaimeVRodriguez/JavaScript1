// If customer has more than 100 points
// customer is gold
// otherwise they are silver

let points = 110;

// Reads as follows
// if condition is true "? then" value is gold
// ": if not" value is silver
let type = points > 100 ? 'gold' : 'silver';

console.log(type)
