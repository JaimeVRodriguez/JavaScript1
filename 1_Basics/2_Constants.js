let interestRate = 0.3
interestRate = 1;
// Last defined variable will be that variables value

console.log(interestRate)

// For variable that shouldn't be reassigned
// Use const
const newRate = 0.3
console.log(newRate)